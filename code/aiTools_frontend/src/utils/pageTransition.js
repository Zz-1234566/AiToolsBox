// =========================================================
// 页面转场工具
// 方案A：Tab 切换用淡入，普通页面跳转用原生转场
// =========================================================

// 跳转 Tab（统一入口，后续可扩展）
export function switchTab(url) {
  uni.switchTab({ url })
}
